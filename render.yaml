services:
  - type: web
    name: shikshasetu
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    disk:
      name: shikshasetu-data
      mountPath: /data
      sizeGB: 1